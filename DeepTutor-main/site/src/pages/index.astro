---
import "../styles/global.css";

const features = [
  {
    chapter: "I",
    title: "Chat Workspace",
    body: "Five modes — Chat, Solve, Quiz, Research, Visualize — share one session, knowledge base, and citation history. Escalate mid-thread without losing context.",
    href: "/docs/explore/chat-workspace/",
  },
  {
    chapter: "II",
    title: "Co-Writer",
    body: "A split-view Markdown workbench where AI is a first-class collaborator. Select, rewrite, expand — grounded in your knowledge base.",
    href: "/docs/explore/co-writer/",
  },
  {
    chapter: "III",
    title: "Book Engine",
    body: "A multi-agent pipeline compiles your materials into interactive living books — 13 block types, including a GeoGebra viewer and animations.",
    href: "/docs/explore/book/",
  },
  {
    chapter: "IV",
    title: "Knowledge",
    body: "Versioned RAG-ready document libraries on LlamaIndex end-to-end. Every (re-)index is tracked, comparable, and rollback-able.",
    href: "/docs/explore/knowledge/",
  },
  {
    chapter: "V",
    title: "Space",
    body: "Your personal library — chat history, notebooks, question bank, and authored skills. The read-and-review counterpart to every active surface.",
    href: "/docs/explore/space/",
  },
  {
    chapter: "VI",
    title: "Memory",
    body: "Append-only L1 traces, L2 curated facts with citations, L3 cross-surface synthesis. A Memory Graph lets you audit why DeepTutor knows what it knows.",
    href: "/docs/explore/memory/",
  },
];

const installs = [
  {
    id: "pypi",
    label: "PyPI",
    tag: "Recommended",
    cmd: "pip install -U deeptutor",
    blurb: "Smoothest path. Full local Web app + CLI, no clone required.",
    code: `# 1. Create a workspace directory
mkdir -p my-deeptutor && cd my-deeptutor

# 2. Install, configure, launch
pip install -U deeptutor
deeptutor init
deeptutor start`,
    foot: "Then open 127.0.0.1:3782",
    href: "/docs/get-started/pypi/",
  },
  {
    id: "docker",
    label: "Docker",
    tag: "One container",
    cmd: "docker run … ghcr.io/hkuds/deeptutor",
    blurb: "One container for the full Web app, from GitHub Container Registry.",
    code: `docker run --rm --name deeptutor \\
  -p 127.0.0.1:3782:3782 \\
  -p 127.0.0.1:8001:8001 \\
  -v deeptutor-data:/app/data \\
  ghcr.io/hkuds/deeptutor:latest`,
    foot: "Map both 3782 (UI) and 8001 (API). Data persists in the named volume.",
    href: "/docs/get-started/docker/",
  },
  {
    id: "source",
    label: "Source",
    tag: "Contributors",
    cmd: "git clone … && pip install -e .",
    blurb: "Hot-reload the frontend, hack on the backend, match CI exactly.",
    code: `git clone https://github.com/HKUDS/DeepTutor.git
cd DeepTutor

python3 -m venv .venv && source .venv/bin/activate
pip install -e .
( cd web && npm ci --legacy-peer-deps )

deeptutor init && deeptutor start`,
    foot: "Python 3.11+ · Node.js 22 LTS. Next.js runs in dev mode against ./web.",
    href: "/docs/get-started/from-source/",
  },
  {
    id: "cli",
    label: "CLI",
    tag: "Headless",
    cmd: "pip install -e ./packaging/deeptutor-cli",
    blurb: "Lightweight install for servers, agent harnesses, Claude Code / Codex setups.",
    code: `git clone https://github.com/HKUDS/DeepTutor.git
cd DeepTutor

python3 -m venv .venv-cli && source .venv-cli/bin/activate
pip install -e ./packaging/deeptutor-cli

deeptutor init --cli
deeptutor chat`,
    foot: "No backend / frontend ports. Embeddings off by default — opt in for RAG.",
    href: "/docs/get-started/cli-only/",
  },
];

---

<!doctype html>
<html lang="en" data-theme="light">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DeepTutor — Agent-native, open-source personalized tutoring</title>
    <meta
      name="description"
      content="An open-source AI learning companion. Five modes, one workspace. Personal TutorBots. Knowledge-driven learning."
    />
    <link rel="icon" type="image/png" href="/logo.png" />
    <script is:inline>
      (() => {
        const key = "starlight-theme";
        const resolve = (preference) =>
          preference === "light" || preference === "dark"
            ? preference
            : window.matchMedia("(prefers-color-scheme: light)").matches
              ? "light"
              : "dark";
        let stored = "auto";
        try {
          const value = localStorage.getItem(key);
          stored = value === "light" || value === "dark" ? value : "auto";
        } catch {}
        document.documentElement.dataset.theme = resolve(stored);
      })();
    </script>
  </head>
  <body>
    <header class="nav">
      <div class="nav-inner">
        <a class="nav-brand" href="/" aria-label="DeepTutor">
          <img src="/logo.png" alt="DeepTutor" />
        </a>
        <nav class="nav-links">
          <a
            class="nav-icon"
            href="https://github.com/HKUDS/DeepTutor"
            target="_blank"
            rel="noopener"
            aria-label="GitHub"
            title="GitHub"
          >
            <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
              <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0 0 16 8c0-4.42-3.58-8-8-8z"></path>
            </svg>
          </a>
          <a
            class="nav-icon"
            href="https://discord.gg/eRsjPgMU4t"
            target="_blank"
            rel="noopener"
            aria-label="Discord"
            title="Discord"
          >
            <svg width="17" height="17" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M19.27 5.33C17.94 4.71 16.5 4.26 15 4a.09.09 0 0 0-.07.03c-.18.33-.39.76-.53 1.09a16.09 16.09 0 0 0-4.8 0c-.14-.34-.35-.76-.54-1.09-.01-.02-.04-.03-.07-.03-1.5.26-2.93.71-4.27 1.33-.01 0-.02.01-.03.02-2.72 4.07-3.47 8.03-3.1 11.95 0 .02.01.04.03.05 1.8 1.32 3.53 2.12 5.24 2.65.03.01.06 0 .07-.02.4-.55.76-1.13 1.07-1.74.02-.04 0-.08-.04-.09-.57-.22-1.11-.48-1.64-.78-.04-.02-.04-.08-.01-.11.11-.08.22-.17.33-.25.02-.02.05-.02.07-.01 3.44 1.57 7.15 1.57 10.55 0 .02-.01.05-.01.07.01.11.09.22.17.33.26.04.03.04.09-.01.11-.52.31-1.07.56-1.64.78-.04.01-.05.06-.04.09.32.61.68 1.19 1.07 1.74.03.01.06.02.09.01 1.72-.53 3.45-1.33 5.25-2.65.02-.01.03-.03.03-.05.44-4.53-.73-8.46-3.1-11.95-.01-.01-.02-.02-.04-.02zM8.52 14.91c-1.03 0-1.89-.95-1.89-2.12s.84-2.12 1.89-2.12c1.06 0 1.9.96 1.89 2.12 0 1.17-.84 2.12-1.89 2.12zm6.97 0c-1.03 0-1.89-.95-1.89-2.12s.84-2.12 1.89-2.12c1.06 0 1.9.96 1.89 2.12 0 1.17-.83 2.12-1.89 2.12z"/>
            </svg>
          </a>
          <a
            class="nav-icon"
            href="https://github.com/HKUDS/DeepTutor/issues/78"
            target="_blank"
            rel="noopener"
            aria-label="WeChat community"
            title="WeChat community"
          >
            <svg width="17" height="17" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M8.69 4C4.55 4 1.2 6.68 1.2 10c0 1.86 1.04 3.51 2.69 4.61l-.7 2.4 2.79-1.5c.83.3 1.74.49 2.7.49.34 0 .67-.02 1-.06-.13-.46-.21-.94-.21-1.43 0-3.07 2.97-5.5 6.7-5.5.33 0 .67.02 1 .07C16.39 6.32 12.86 4 8.69 4ZM6.18 8.5a1 1 0 1 1 0-2 1 1 0 0 1 0 2Zm5 0a1 1 0 1 1 0-2 1 1 0 0 1 0 2Zm5 1c-3.3 0-6 2.24-6 5s2.7 5 6 5c.74 0 1.45-.12 2.1-.34L20.2 20l-.7-2.05c1.07-.95 1.7-2.15 1.7-3.45 0-2.76-2.7-5-6-5Zm-2 4a.75.75 0 1 1 0-1.5.75.75 0 0 1 0 1.5Zm4 0a.75.75 0 1 1 0-1.5.75.75 0 0 1 0 1.5Z"/>
            </svg>
          </a>
          <span class="nav-tool-divider nav-links-divider" aria-hidden="true"></span>
          <a class="nav-text" href="/docs/">Docs</a>
        </nav>
        <div class="nav-tools" aria-label="Preferences">
          <label class="nav-tool" aria-label="Theme">
            <svg class="nav-tool-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="2.5" y="4" width="19" height="13" rx="2"/>
              <path d="M8 21h8M12 17v4"/>
            </svg>
            <select class="nav-select" data-theme-select aria-label="Theme">
              <option value="auto">Auto</option>
              <option value="light">Light</option>
              <option value="dark">Dark</option>
            </select>
            <svg class="nav-tool-caret" width="10" height="10" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <polyline points="3 4.5 6 7.5 9 4.5"/>
            </svg>
          </label>
          <span class="nav-tool-divider" aria-hidden="true"></span>
          <label class="nav-tool" aria-label="Language">
            <svg class="nav-tool-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <circle cx="12" cy="12" r="9.5"/>
              <path d="M2.5 12h19"/>
              <path d="M12 2.5a14 14 0 0 1 4 9.5 14 14 0 0 1-4 9.5 14 14 0 0 1-4-9.5 14 14 0 0 1 4-9.5z"/>
            </svg>
            <select class="nav-select" data-locale-select aria-label="Language">
              <option value="/" selected>English</option>
              <option value="/zh-cn/">简体中文</option>
            </select>
            <svg class="nav-tool-caret" width="10" height="10" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <polyline points="3 4.5 6 7.5 9 4.5"/>
            </svg>
          </label>
        </div>
      </div>
    </header>

    <main>
      <section class="hero">
        <div class="hero-bg" aria-hidden="true"></div>
        <img class="hero-wordmark" src="/banner.png" alt="DeepTutor" />
        <h1 class="hero-tagline">
          Personal tutoring,<br />
          <em>reimagined.</em>
        </h1>
        <p class="hero-sub">
          Agent-native Learning Companion that grows with you.<br />
          Fully Open-Sourced.
        </p>
        <div class="hero-actions">
          <a class="btn btn-primary" href="/docs/">Docs</a>
          <a
            class="btn btn-ghost btn-gh"
            href="https://github.com/HKUDS/DeepTutor"
            target="_blank"
            rel="noopener"
            aria-label="DeepTutor on GitHub"
          >
            <svg width="15" height="15" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
              <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0 0 16 8c0-4.42-3.58-8-8-8z"></path>
            </svg>
            <span>GitHub</span>
            <span class="btn-gh-sep" aria-hidden="true"></span>
            <span class="btn-gh-count" data-gh-stars>—</span>
            <span class="btn-gh-label">Stars</span>
          </a>
        </div>
        <div class="hero-meta">
          <span><strong>Apache 2.0</strong></span>
          <span class="sep">·</span>
          <span>From <strong>HKU Data Intelligence Lab</strong></span>
        </div>
      </section>

      <section class="showcase">
        <div class="showcase-frame">
          <div class="showcase-chrome">
            <span class="dot red"></span>
            <span class="dot amber"></span>
            <span class="dot green"></span>
            <span class="showcase-title">DeepTutor — Chat Workspace</span>
          </div>
          <img
            class="showcase-shot"
            src="/screenshots/dt-home.png"
            alt="DeepTutor chat workspace — Chat / TutorBot / Co-Writer / Book / Knowledge / Space / Memory in the sidebar; greeting and composer in the main area."
            loading="eager"
            decoding="async"
          />
        </div>
      </section>

      <section class="section">
        <header class="section-head">
          <span class="section-eyebrow">What's inside</span>
          <h2>Six pillars, <em>one</em> running context.</h2>
          <p>
            Every surface reads from — and writes back to — the same memory, knowledge base, and tutor.
            Switching contexts no longer means losing your place.
          </p>
        </header>
        <div class="feature-grid">
          {
            features.map((f) => (
              <a class="feature" href={f.href}>
                <span class="feature-mark">{f.chapter}</span>
                <h3>{f.title}</h3>
                <p>{f.body}</p>
                <span class="feature-arrow">
                  <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
                    <path
                      d="M3 11L11 3M11 3H5M11 3V9"
                      stroke="currentColor"
                      stroke-width="1.5"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                    />
                  </svg>
                </span>
              </a>
            ))
          }
        </div>
      </section>

      <section class="section section-quiet">
        <header class="section-head">
          <span class="section-eyebrow">Install</span>
          <h2>Up and running in <em>minutes</em>.</h2>
          <p>
            Four ways to install — pick the one that fits.
            Every path lands at the same configured workspace.
          </p>
        </header>
        <div class="install" id="install" data-active="pypi">
          <div class="install-tabs" role="tablist">
            {
              installs.map((opt, i) => (
                <button
                  class={`install-tab${i === 0 ? " is-active" : ""}`}
                  role="tab"
                  aria-selected={i === 0 ? "true" : "false"}
                  data-target={opt.id}
                >
                  <span class="install-tab-label">{opt.label}</span>
                </button>
              ))
            }
          </div>
          <div class="install-body">
            {
              installs.map((opt, i) => (
                <div
                  class={`install-panel${i === 0 ? " is-active" : ""}`}
                  role="tabpanel"
                  data-panel={opt.id}
                >
                  <p class="install-blurb">{opt.blurb}</p>
                  <pre class="install-code"><code>{opt.code}</code></pre>
                  <div class="install-foot">
                    <span class="install-foot-note">{opt.foot}</span>
                    <a class="install-foot-link" href={opt.href}>
                      Full guide
                      <svg width="12" height="12" viewBox="0 0 14 14" fill="none" aria-hidden="true">
                        <path d="M3 11L11 3M11 3H5M11 3V9" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </a>
                  </div>
                </div>
              ))
            }
          </div>
        </div>
      </section>

      <footer class="footer">
        <div class="footer-inner">
          <a class="footer-brand" href="/">
            <img src="/logo.png" alt="" />
            <span>DeepTutor</span>
          </a>
          <div class="footer-cols">
            <div class="footer-col">
              <h4>Docs</h4>
              <a href="/docs/get-started/">Get Started</a>
              <a href="/docs/explore/">Explore DeepTutor</a>
              <a href="/docs/tutorbot/">Explore TutorBot</a>
              <a href="/docs/cli/">CLI</a>
            </div>
            <div class="footer-col">
              <h4>Community</h4>
              <a href="https://discord.gg/eRsjPgMU4t" target="_blank" rel="noopener">Discord</a>
              <a href="https://github.com/HKUDS/DeepTutor/issues/78" target="_blank" rel="noopener">
                WeChat
              </a>
              <a href="https://github.com/HKUDS/DeepTutor/discussions" target="_blank" rel="noopener">
                Discussions
              </a>
              <a href="https://github.com/HKUDS/DeepTutor/issues" target="_blank" rel="noopener">
                Issues
              </a>
            </div>
            <div class="footer-col">
              <h4>Project</h4>
              <a href="https://github.com/HKUDS/DeepTutor" target="_blank" rel="noopener">
                GitHub
              </a>
              <a
                href="https://github.com/HKUDS/DeepTutor/blob/main/LICENSE"
                target="_blank"
                rel="noopener">License</a
              >
              <a href="https://arxiv.org/abs/2604.26962" target="_blank" rel="noopener">arXiv</a>
            </div>
          </div>
        </div>
        <div class="footer-rule"></div>

        <div class="footer-credo">
          <p class="footer-tagline">
            From the <em>community</em>, to <em>the world</em>.
          </p>
          <div class="footer-pulse" id="footer-pulse" data-hidden="true" aria-live="polite">
            <span class="footer-pulse-dot" aria-hidden="true"></span>
            Latest:
            <a class="footer-pulse-tag" data-pulse="release-link" href="https://github.com/HKUDS/DeepTutor/releases" target="_blank" rel="noopener">
              <span data-pulse="release-tag">—</span>
            </a>
            <span class="sep">·</span>
            <span class="num"><span data-pulse="requests">—</span> Page Views</span>
          </div>
        </div>

        <div class="footer-fine">
          <span class="footer-fine-credit">
            © 2026 <a href="https://github.com/HKUDS" target="_blank" rel="noopener">
              Data Intelligence Lab @ HKU
            </a>
          </span>
          <span class="footer-fine-license">Released under the Apache 2.0 License</span>
        </div>
      </footer>

      <script is:inline>
        (async () => {
          const pulse = document.getElementById("footer-pulse");
          if (!pulse) return;

          // Number formatters.
          const fmtNum = (n) => {
            if (n == null) return "0";
            if (n >= 1e9) return (n / 1e9).toFixed(2).replace(/\.?0+$/, "") + "B";
            if (n >= 1e6) return (n / 1e6).toFixed(2).replace(/\.?0+$/, "") + "M";
            if (n >= 1e3) return (n / 1e3).toFixed(2).replace(/\.?0+$/, "") + "k";
            return String(n);
          };
          const fmtBytes = (b) => {
            if (b == null) return "0 B";
            const KB = 1024, MB = KB * 1024, GB = MB * 1024, TB = GB * 1024;
            if (b >= TB) return (b / TB).toFixed(2) + " TB";
            if (b >= GB) return (b / GB).toFixed(2) + " GB";
            if (b >= MB) return (b / MB).toFixed(2) + " MB";
            if (b >= KB) return (b / KB).toFixed(2) + " KB";
            return b + " B";
          };
          const setText = (key, value) => {
            const el = pulse.querySelector('[data-pulse="' + key + '"]');
            if (el) el.textContent = value;
          };

          // On localhost (astro preview / dev), Pages Functions are not
          // available — substitute a sample so the heartbeat is visible.
          // Production fetches the real APIs.
          const isLocal =
            location.hostname === "localhost" ||
            location.hostname === "127.0.0.1" ||
            location.hostname === "0.0.0.0";

          const sample = isLocal
            ? {
                traffic: {
                  window: "total",
                  requests: 78400,
                  bytes: 1530000000,
                  uniques: 18200,
                },
                release: {
                  tag: "v1.4.0-beta",
                  name: "v1.4.0-beta",
                  url: "https://github.com/HKUDS/DeepTutor/releases/tag/v1.4.0-beta",
                  published_at: "2026-05-21T00:00:00Z",
                  prerelease: true,
                },
              }
            : null;

          const [traffic, release] = await Promise.allSettled([
            isLocal
              ? Promise.resolve(sample.traffic)
              : fetch("/api/traffic", { cache: "no-store" }).then((r) =>
                  r.ok ? r.json() : Promise.reject(new Error("status=" + r.status)),
                ),
            isLocal
              ? Promise.resolve(sample.release)
              : fetch("/api/release", { cache: "no-store" }).then((r) =>
                  r.ok ? r.json() : Promise.reject(new Error("status=" + r.status)),
                ),
          ]);

          let trafficOK = false;
          let releaseOK = false;

          if (traffic.status === "fulfilled" && traffic.value && !traffic.value.error) {
            setText("requests", fmtNum(traffic.value.requests));
            setText("bytes", fmtBytes(traffic.value.bytes));
            trafficOK = true;
          }
          if (release.status === "fulfilled" && release.value && !release.value.error) {
            setText("release-tag", release.value.tag);
            const link = pulse.querySelector('[data-pulse="release-link"]');
            if (link && release.value.url) link.setAttribute("href", release.value.url);
            releaseOK = true;
          }

          if (releaseOK || trafficOK) {
            pulse.setAttribute("data-hidden", "false");
          }
        })();
      </script>
    </main>

    <style>
      /* ───────── Nav ───────── */
      .nav {
        position: sticky;
        top: 0;
        z-index: 50;
        background: rgba(250, 248, 243, 0.78);
        backdrop-filter: saturate(180%) blur(14px);
        -webkit-backdrop-filter: saturate(180%) blur(14px);
        border-bottom: 1px solid var(--rule);
      }
      .nav-inner {
        max-width: 1280px;
        margin: 0 auto;
        padding: 3px 32px;
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .nav-brand {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 30px;
        height: 30px;
        text-decoration: none;
        color: var(--ink);
      }
      .nav-brand img {
        width: 22px;
        height: 22px;
        display: block;
        object-fit: contain;
      }
      .nav-links {
        display: flex;
        align-items: center;
        gap: 2px;
        margin-left: 6px;
      }
      .nav-icon {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 30px;
        height: 30px;
        color: var(--ink-soft);
        border-radius: 8px;
        text-decoration: none;
        transition: color 0.15s ease, background 0.15s ease;
      }
      .nav-icon svg {
        width: 17px;
        height: 17px;
      }
      .nav-icon:hover {
        color: var(--ink);
        background: rgba(24, 24, 28, 0.05);
      }
      .nav-text {
        text-decoration: none;
        color: var(--ink-soft);
        font-size: 14px;
        font-weight: 500;
        padding: 0 6px;
        transition: color 0.15s ease;
      }
      .nav-text:hover {
        color: var(--ink);
      }
      .nav-links-divider {
        margin: 0 8px;
      }
      .nav-tools {
        margin-left: auto;
        display: flex;
        align-items: center;
        gap: 12px;
      }
      .nav-tool {
        position: relative;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        color: var(--ink-soft);
        font-size: 13.5px;
        font-weight: 500;
        cursor: pointer;
        transition: color 0.15s ease;
      }
      .nav-tool:hover {
        color: var(--ink);
      }
      .nav-tool-icon {
        flex-shrink: 0;
        color: var(--ink-faint);
        transition: color 0.15s ease;
      }
      .nav-tool:hover .nav-tool-icon {
        color: var(--ink-soft);
      }
      .nav-select {
        appearance: none;
        -webkit-appearance: none;
        border: 0;
        background: transparent;
        color: inherit;
        font: inherit;
        cursor: pointer;
        padding: 4px 16px 4px 0;
        outline: none;
        line-height: 1;
      }
      .nav-tool-caret {
        position: absolute;
        right: 0;
        pointer-events: none;
        opacity: 0.5;
      }
      .nav-tool-divider {
        width: 1px;
        height: 14px;
        background: var(--rule-strong);
        opacity: 0.6;
      }
      @media (max-width: 900px) {
        .nav-links {
          display: none;
        }
      }
      @media (max-width: 620px) {
        .nav-inner {
          padding: 3px 16px;
          gap: 8px;
        }
      }


      /* ───────── Hero ───────── */
      .hero {
        position: relative;
        max-width: 1280px;
        margin: 0 auto;
        padding: 96px 32px 80px;
        text-align: center;
        overflow: hidden;
      }
      .hero-bg {
        position: absolute;
        top: 60px;
        left: 50%;
        transform: translateX(-50%);
        width: 760px;
        height: 380px;
        background: var(--warm-glow);
        filter: blur(40px);
        z-index: -1;
        pointer-events: none;
      }
      .eyebrow {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 36px;
        padding: 6px 14px;
        border: 1px solid var(--rule);
        background: rgba(250, 248, 243, 0.6);
        border-radius: 999px;
        font-family: var(--font-mono);
        font-size: 11.5px;
        font-weight: 500;
        letter-spacing: 0.04em;
        color: var(--ink-soft);
      }
      .eyebrow .dot {
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: var(--accent);
        box-shadow: 0 0 0 3px var(--accent-soft);
      }
      .hero-wordmark {
        display: block;
        max-width: 380px;
        width: min(72%, 380px);
        height: auto;
        margin: 0 auto 32px;
        user-select: none;
      }
      .hero-tagline {
        font-family: var(--font-display);
        font-weight: 500;
        font-size: clamp(36px, 5.4vw, 60px);
        line-height: 1.05;
        letter-spacing: -0.03em;
        color: var(--ink);
        margin: 0 auto 24px;
        max-width: 760px;
        font-variation-settings: "opsz" 72, "SOFT" 30;
      }
      .hero-tagline em {
        font-style: italic;
        font-weight: 400;
        color: var(--accent);
        font-variation-settings: "opsz" 72, "SOFT" 100;
      }
      .hero-sub {
        font-size: 17px;
        line-height: 1.55;
        color: var(--ink-soft);
        margin: 0 auto 40px;
        max-width: 560px;
        font-weight: 400;
      }
      .hero-actions {
        display: flex;
        gap: 10px;
        justify-content: center;
        flex-wrap: wrap;
        margin-bottom: 56px;
      }
      .btn {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        height: 46px;
        padding: 0 22px;
        border-radius: 999px;
        font-size: 14.5px;
        font-weight: 500;
        text-decoration: none;
        transition: transform 0.15s ease, background 0.15s ease,
          border-color 0.15s ease, box-shadow 0.15s ease, color 0.15s ease;
        border: 1px solid transparent;
      }
      .btn-primary {
        background: var(--ink);
        color: var(--paper);
      }
      .btn-primary:hover {
        background: #000;
        transform: translateY(-1px);
        box-shadow: 0 10px 28px -10px rgba(24, 24, 28, 0.5);
      }
      .btn-ghost {
        background: transparent;
        border-color: var(--rule-strong);
        color: var(--ink);
      }
      .btn-ghost:hover {
        border-color: var(--ink);
        transform: translateY(-1px);
      }
      .btn-gh {
        gap: 9px;
        padding: 0 18px;
        font-family: var(--font-mono);
        font-size: 13.5px;
      }
      .btn-gh svg {
        flex-shrink: 0;
      }
      .btn-gh-sep {
        width: 1px;
        height: 18px;
        background: var(--rule-strong);
        opacity: 0.8;
      }
      .btn-gh-count {
        font-family: var(--font-mono);
        font-weight: 600;
        font-size: 13.5px;
        letter-spacing: 0.01em;
        color: var(--ink);
        min-width: 1.5ch;
        text-align: right;
      }
      .btn-gh-label {
        font-family: var(--font-mono);
        color: var(--ink-soft);
        font-weight: 500;
      }
      .btn-gh:hover .btn-gh-count {
        color: var(--ink);
      }
      .hero-meta {
        font-family: var(--font-mono);
        font-size: 11.5px;
        color: var(--ink-faint);
        letter-spacing: 0.02em;
        display: inline-flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 8px;
      }
      .hero-meta strong {
        color: var(--ink-soft);
        font-weight: 600;
      }
      .hero-meta .sep {
        opacity: 0.4;
      }

      /* ───────── Product showcase ───────── */
      .showcase {
        max-width: 1180px;
        margin: 0 auto 16px;
        padding: 0 32px;
      }
      .showcase-frame {
        border-radius: 20px;
        overflow: hidden;
        border: 1px solid var(--rule-strong);
        background: var(--paper-soft);
        box-shadow:
          0 1px 0 rgba(255, 255, 255, 0.6) inset,
          0 40px 80px -40px rgba(24, 24, 28, 0.22),
          0 16px 32px -20px rgba(24, 24, 28, 0.1);
      }
      .showcase-chrome {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 11px 16px;
        border-bottom: 1px solid var(--rule);
        background: rgba(250, 248, 243, 0.7);
      }
      .showcase-chrome .dot {
        width: 11px;
        height: 11px;
        border-radius: 50%;
      }
      .showcase-chrome .dot.red {
        background: #f15a55;
      }
      .showcase-chrome .dot.amber {
        background: #f9bb3f;
      }
      .showcase-chrome .dot.green {
        background: #4dc44d;
      }
      .showcase-title {
        margin-left: 12px;
        font-family: var(--font-mono);
        font-size: 11.5px;
        color: var(--ink-faint);
        letter-spacing: 0.02em;
      }
      .showcase-frame img {
        width: 100%;
        display: block;
      }

      /* ───────── Section ───────── */
      .section {
        position: relative;
        max-width: 1180px;
        margin: 0 auto;
        padding: 128px 32px;
      }
      .section-quiet {
        background: var(--paper-soft);
        max-width: none;
        margin: 96px 0 0;
        padding-left: 32px;
        padding-right: 32px;
      }
      .section-quiet > .chapter-mark,
      .section-quiet > .section-head,
      .section-quiet > .install {
        max-width: 1180px;
        margin-left: auto;
        margin-right: auto;
      }
      .chapter-mark {
        font-family: var(--font-display);
        font-style: italic;
        font-size: 14px;
        font-weight: 500;
        color: var(--ink-faint);
        text-align: center;
        letter-spacing: 0.04em;
        margin-bottom: 24px;
        font-variation-settings: "opsz" 24;
      }
      .section-head {
        text-align: center;
        margin-bottom: 72px;
        max-width: 720px;
        margin-left: auto;
        margin-right: auto;
      }
      .section-eyebrow {
        display: inline-block;
        margin-bottom: 18px;
        font-family: var(--font-mono);
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: var(--accent);
      }
      .section-head h2 {
        font-family: var(--font-display);
        margin: 0 0 16px;
        font-size: clamp(34px, 4.4vw, 52px);
        font-weight: 500;
        letter-spacing: -0.032em;
        line-height: 1.05;
        color: var(--ink);
        font-variation-settings: "opsz" 56, "SOFT" 30;
      }
      .section-head h2 em {
        font-style: italic;
        font-weight: 400;
        color: var(--ink-soft);
        font-variation-settings: "opsz" 56, "SOFT" 100;
      }
      .section-head p {
        margin: 0 auto;
        max-width: 580px;
        font-size: 17px;
        color: var(--ink-soft);
        line-height: 1.55;
      }

      /* ───────── Feature grid ───────── */
      .feature-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        border: 1px solid var(--rule);
        border-radius: 16px;
        overflow: hidden;
        background: var(--paper-soft);
        gap: 1px;
      }
      .feature {
        position: relative;
        display: flex;
        flex-direction: column;
        padding: 44px 36px 48px;
        background: var(--paper);
        text-decoration: none;
        color: var(--ink);
        transition: background 0.25s ease, transform 0.2s ease;
      }
      .feature:hover {
        background: rgba(77, 143, 191, 0.04);
      }
      .feature-mark {
        font-family: var(--font-display);
        font-style: italic;
        font-size: 26px;
        font-weight: 400;
        line-height: 1;
        color: var(--accent);
        margin-bottom: 28px;
        font-variation-settings: "opsz" 36;
      }
      .feature h3 {
        font-family: var(--font-display);
        margin: 0 0 12px;
        font-size: 22px;
        font-weight: 500;
        letter-spacing: -0.02em;
        color: var(--ink);
        font-variation-settings: "opsz" 28;
      }
      .feature p {
        margin: 0;
        font-size: 14.5px;
        line-height: 1.65;
        color: var(--ink-soft);
        flex: 1;
      }
      .feature-arrow {
        opacity: 0;
        transform: translate(-4px, 4px);
        transition: opacity 0.2s ease, transform 0.25s ease;
        margin-top: 24px;
        color: var(--accent);
      }
      .feature:hover .feature-arrow {
        opacity: 1;
        transform: translate(0, 0);
      }
      @media (max-width: 980px) {
        .feature-grid {
          grid-template-columns: repeat(2, 1fr);
        }
      }
      @media (max-width: 640px) {
        .feature-grid {
          grid-template-columns: 1fr;
        }
      }

      /* ───────── Install (editorial inline tabs) ───────── */
      .install {
        max-width: 820px;
        margin: 0 auto;
      }
      .install-tabs {
        display: flex;
        align-items: center;
        gap: 2px;
        padding: 4px;
        margin: 0 auto 28px;
        width: fit-content;
        border-radius: 999px;
        background: rgba(24, 24, 28, 0.045);
        border: 1px solid rgba(24, 24, 28, 0.04);
      }
      .install-tab {
        appearance: none;
        background: transparent;
        border: 0;
        cursor: pointer;
        padding: 7px 16px;
        border-radius: 999px;
        font-family: inherit;
        font-size: 13px;
        font-weight: 500;
        letter-spacing: -0.005em;
        color: var(--ink-soft);
        transition:
          color 0.18s ease,
          background 0.22s ease,
          box-shadow 0.22s ease;
      }
      .install-tab:hover {
        color: var(--ink);
      }
      .install-tab.is-active {
        background: var(--paper);
        color: var(--ink);
        box-shadow:
          0 1px 1px rgba(24, 24, 28, 0.04),
          0 2px 8px -2px rgba(24, 24, 28, 0.08);
      }
      .install-tab-label {
        display: inline-block;
      }
      .install-body {
        background: var(--paper);
        border: 1px solid var(--rule);
        border-radius: 14px;
        padding: 28px 30px 24px;
        box-shadow: 0 1px 0 rgba(255, 255, 255, 0.6) inset;
      }
      .install-panel {
        display: none;
        animation: install-fade 0.25s ease;
      }
      .install-panel.is-active {
        display: block;
      }
      @keyframes install-fade {
        from { opacity: 0; transform: translateY(2px); }
        to   { opacity: 1; transform: translateY(0); }
      }
      .install-blurb {
        margin: 0 0 18px;
        font-size: 14px;
        color: var(--ink-soft);
        line-height: 1.55;
      }
      .install-code {
        margin: 0;
        padding: 20px 22px;
        background: rgba(244, 240, 232, 0.55);
        border: 1px solid var(--rule);
        border-radius: 10px;
        font-family: var(--font-mono);
        font-size: 13px;
        line-height: 1.78;
        color: var(--ink);
        overflow-x: auto;
        white-space: pre;
      }
      .install-code code {
        font-family: inherit;
        font-size: inherit;
        color: inherit;
        background: transparent;
        padding: 0;
      }
      .install-foot {
        margin: 16px 2px 0;
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        align-items: center;
        gap: 12px;
        font-size: 13.5px;
        color: var(--ink-soft);
      }
      .install-foot-note {
        font-family: var(--font-mono);
        font-size: 12px;
        color: var(--ink-faint);
        letter-spacing: 0.01em;
      }
      .install-foot-link {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        color: var(--ink);
        text-decoration: none;
        font-weight: 500;
        font-size: 13.5px;
        border-bottom: 1px solid var(--rule-strong);
        padding-bottom: 1px;
        transition: border-color 0.15s ease, color 0.15s ease;
      }
      .install-foot-link:hover {
        border-color: var(--ink);
      }
      .install-foot-link svg {
        transition: transform 0.2s ease;
      }
      .install-foot-link:hover svg {
        transform: translate(1px, -1px);
      }
      @media (max-width: 760px) {
        .install-tab {
          flex: 1;
          min-width: 0;
          padding: 9px 8px 11px;
        }
      }

      /* ───────── Ecosystem ───────── */
      .section-eco {
        padding-top: 96px;
        padding-bottom: 64px;
      }
      .section-eco .section-head {
        margin-bottom: 48px;
      }
      .eco-list {
        max-width: 820px;
        margin: 0 auto;
        border-top: 1px solid var(--rule);
      }
      .eco-row {
        display: grid;
        grid-template-columns: 180px 1fr auto;
        align-items: center;
        gap: 24px;
        padding: 18px 8px;
        border-bottom: 1px solid var(--rule);
        text-decoration: none;
        color: var(--ink);
        transition: padding 0.2s ease, background 0.18s ease;
      }
      .eco-row:hover {
        padding-left: 16px;
        background: rgba(77, 143, 191, 0.025);
      }
      .eco-name {
        font-family: var(--font-display);
        font-style: italic;
        font-size: 19px;
        font-weight: 500;
        letter-spacing: -0.014em;
        color: var(--ink);
        font-variation-settings: "opsz" 24;
      }
      .eco-desc {
        font-size: 14px;
        color: var(--ink-soft);
        line-height: 1.5;
      }
      .eco-arrow {
        color: var(--ink-faint);
        transition: color 0.18s ease, transform 0.22s ease;
        display: inline-flex;
      }
      .eco-row:hover .eco-arrow {
        color: var(--accent);
        transform: translate(2px, -2px);
      }
      @media (max-width: 720px) {
        .eco-row {
          grid-template-columns: 1fr auto;
          gap: 6px 12px;
          padding: 16px 8px;
        }
        .eco-desc {
          grid-column: 1 / -1;
        }
      }

      /* ───────── Footer credo — tagline + heartbeat, centered ───────── */
      .footer-credo {
        text-align: center;
        margin: 0 0 32px;
      }
      .footer-tagline {
        font-family: var(--font-display);
        font-size: 22px;
        font-weight: 500;
        letter-spacing: -0.02em;
        line-height: 1.25;
        color: var(--ink);
        margin: 0 0 14px;
        font-variation-settings: "opsz" 32;
      }
      .footer-tagline em {
        font-style: italic;
        font-weight: 400;
        color: var(--ink-soft);
      }

      /* ───────── Interactive app mock ───────── */
      .dt-app {
        display: grid;
        grid-template-columns: 220px 1fr;
        background: var(--paper);
        height: 620px;
        max-height: 78vh;
        font-size: 13.5px;
        color: var(--ink);
      }

      /* sidebar */
      .dt-side {
        display: flex;
        flex-direction: column;
        background: #f1ece1;
        border-right: 1px solid var(--rule);
        min-width: 0;
      }
      .dt-side-head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 14px;
        height: 56px;
      }
      .dt-side-brand {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        text-decoration: none;
      }
      .dt-side-logo {
        width: 22px;
        height: 22px;
      }
      .dt-side-banner {
        height: 22px;
        width: auto;
      }
      .dt-side-collapse {
        appearance: none;
        background: transparent;
        border: 0;
        padding: 4px;
        border-radius: 6px;
        color: var(--ink-faint);
        cursor: pointer;
        transition: background 0.15s ease, color 0.15s ease;
      }
      .dt-side-collapse svg {
        width: 15px;
        height: 15px;
      }
      .dt-side-collapse:hover {
        background: rgba(24, 24, 28, 0.06);
        color: var(--ink);
      }
      .dt-side-nav {
        padding: 4px 8px;
        display: flex;
        flex-direction: column;
        gap: 1px;
        min-height: 0;
        overflow-y: auto;
      }
      .dt-side-nav::-webkit-scrollbar {
        width: 6px;
      }
      .dt-side-nav::-webkit-scrollbar-thumb {
        background: rgba(24, 24, 28, 0.1);
        border-radius: 3px;
      }
      .dt-side-item {
        appearance: none;
        background: transparent;
        border: 0;
        font: inherit;
        text-align: left;
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 7px 12px;
        border-radius: 8px;
        color: var(--ink-soft);
        cursor: pointer;
        transition: background 0.15s ease, color 0.15s ease;
        font-size: 13.5px;
      }
      .dt-side-item:hover {
        background: rgba(255, 252, 247, 0.65);
        color: var(--ink);
      }
      .dt-side-item.is-active {
        background: rgba(255, 252, 247, 0.85);
        color: var(--ink);
        font-weight: 500;
      }
      .dt-side-icon {
        width: 16px;
        height: 16px;
        flex-shrink: 0;
        color: currentColor;
      }
      .dt-side-newchat {
        color: var(--ink-soft);
      }
      .dt-side-sublist {
        margin: 2px 0 6px 18px;
        padding-left: 8px;
        border-left: 1px solid rgba(24, 24, 28, 0.08);
        display: flex;
        flex-direction: column;
        gap: 1px;
      }
      .dt-side-sub-label {
        font-family: var(--font-mono);
        font-size: 10.5px;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--ink-faint);
        padding: 6px 6px 2px;
      }
      .dt-side-sub-item {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 4px 6px;
        border-radius: 6px;
        color: var(--ink-soft);
        text-decoration: none;
        font-size: 12.5px;
        transition: background 0.15s ease, color 0.15s ease;
      }
      .dt-side-sub-item:hover {
        background: rgba(255, 252, 247, 0.7);
        color: var(--ink);
      }
      .dt-side-dot {
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: #5ec48a;
        box-shadow: 0 0 0 2px rgba(94, 196, 138, 0.18);
        flex-shrink: 0;
      }
      .dt-side-sub-text {
        flex: 1;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .dt-side-sub-flat {
        justify-content: space-between;
        padding-left: 10px;
      }
      .dt-side-sub-meta {
        font-family: var(--font-mono);
        font-size: 10.5px;
        color: var(--ink-faint);
        margin-left: 8px;
      }
      .dt-side-foot {
        margin-top: auto;
        border-top: 1px solid rgba(24, 24, 28, 0.06);
        padding: 8px;
        display: flex;
        flex-direction: column;
        gap: 4px;
      }
      .dt-side-version {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 4px 12px 2px;
      }
      .dt-side-version-tag {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--ink-faint);
        letter-spacing: 0.02em;
      }
      .dt-side-gh {
        display: inline-flex;
        width: 22px;
        height: 22px;
        align-items: center;
        justify-content: center;
        border-radius: 6px;
        color: rgba(24, 24, 28, 0.4);
        transition: color 0.15s ease, background 0.15s ease;
      }
      .dt-side-gh:hover {
        color: var(--ink);
        background: rgba(255, 252, 247, 0.7);
      }
      .dt-side-gh svg {
        width: 13px;
        height: 13px;
      }

      /* main column */
      .dt-main {
        display: flex;
        flex-direction: column;
        min-width: 0;
        background: var(--paper);
      }
      .dt-topbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 13px 24px;
        border-bottom: 1px solid var(--rule);
        gap: 16px;
      }
      .dt-thread-title {
        font-size: 14px;
        font-weight: 600;
        color: var(--ink);
      }
      .dt-topbar-actions {
        display: flex;
        gap: 6px;
        flex-wrap: wrap;
      }
      .dt-act {
        appearance: none;
        background: transparent;
        border: 1px solid rgba(24, 24, 28, 0.1);
        color: var(--ink-soft);
        font-family: inherit;
        font-size: 12px;
        font-weight: 500;
        padding: 6px 11px;
        border-radius: 8px;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        cursor: pointer;
        transition: background 0.15s ease, border-color 0.15s ease, color 0.15s ease;
      }
      .dt-act svg {
        width: 13px;
        height: 13px;
      }
      .dt-act:hover {
        border-color: rgba(24, 24, 28, 0.22);
        color: var(--ink);
      }
      .dt-act.is-muted {
        color: var(--ink-faint);
        cursor: not-allowed;
        border-color: rgba(24, 24, 28, 0.08);
      }
      .dt-act.is-muted:hover {
        border-color: rgba(24, 24, 28, 0.08);
        color: var(--ink-faint);
      }
      .dt-canvas {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 24px 24px 48px;
        gap: 30px;
      }
      .dt-greeting {
        display: inline-flex;
        align-items: center;
        gap: 14px;
      }
      .dt-greeting-mark {
        width: 36px;
        height: 36px;
        user-select: none;
      }
      .dt-greeting h1 {
        margin: 0;
        font-family: var(--font-display);
        font-size: clamp(26px, 3.4vw, 40px);
        font-weight: 500;
        letter-spacing: -0.018em;
        line-height: 1.1;
        color: var(--ink);
        font-variation-settings: "opsz" 44;
      }

      /* composer */
      .dt-composer {
        width: 100%;
        max-width: 640px;
        background: var(--paper);
        border: 1px solid rgba(24, 24, 28, 0.1);
        border-radius: 26px;
        padding: 14px 18px 10px;
        box-shadow:
          0 1px 2px rgba(24, 24, 28, 0.025),
          0 10px 28px -10px rgba(24, 24, 28, 0.08);
        transition: border-color 0.18s ease, box-shadow 0.18s ease;
      }
      .dt-composer:focus-within {
        border-color: var(--accent);
        box-shadow: 0 0 0 4px var(--accent-soft);
      }
      .dt-composer-input {
        width: 100%;
        border: 0;
        outline: 0;
        resize: none;
        background: transparent;
        font-family: inherit;
        font-size: 15px;
        line-height: 1.55;
        color: var(--ink);
        padding: 4px 0 6px;
      }
      .dt-composer-input::placeholder {
        color: var(--ink-faint);
      }
      .dt-composer-row {
        display: flex;
        align-items: center;
        gap: 6px;
        margin-top: 4px;
        flex-wrap: nowrap;
      }
      .dt-composer-spacer {
        flex: 1;
      }
      /* "Chat ▼" capability pill — bordered, full pill with label and caret */
      .dt-pill {
        appearance: none;
        background: var(--paper);
        border: 1px solid rgba(24, 24, 28, 0.12);
        border-radius: 999px;
        height: 28px;
        padding: 0 10px 0 11px;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        font-size: 13px;
        font-weight: 500;
        color: var(--ink);
        cursor: pointer;
        transition: border-color 0.15s ease, background 0.15s ease, box-shadow 0.15s ease;
        box-shadow: 0 1px 2px rgba(24, 24, 28, 0.03);
      }
      .dt-pill:hover {
        border-color: rgba(24, 24, 28, 0.22);
      }
      .dt-pill-left {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        min-width: 0;
      }
      .dt-pill-icon,
      .dt-pill svg {
        width: 13px;
        height: 13px;
        color: var(--ink-soft);
        flex-shrink: 0;
      }
      .dt-pill-caret {
        opacity: 0.55;
      }
      .dt-pill-mode {
        min-width: 110px;
        justify-content: space-between;
      }
      .dt-pill-mode.is-pulsing {
        animation: dt-pulse 0.45s ease;
      }
      @keyframes dt-pulse {
        0% { transform: scale(1); }
        50% { transform: scale(1.03); }
        100% { transform: scale(1); }
      }
      .dt-pill-model {
        gap: 8px;
        max-width: 160px;
      }
      .dt-pill-model .dt-pill-label {
        max-width: 110px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }
      .dt-model-dot {
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: linear-gradient(135deg, #d8a974, #c8865e);
        box-shadow: 0 0 0 2px rgba(216, 169, 116, 0.18);
        flex-shrink: 0;
      }
      /* "Attach", "Knowledge ▼", "Space ▼" — ghost tools */
      .dt-tool {
        appearance: none;
        background: transparent;
        border: 0;
        color: var(--ink-soft);
        font: inherit;
        font-size: 12.5px;
        font-weight: 500;
        padding: 6px 8px;
        border-radius: 8px;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        cursor: pointer;
        transition: background 0.15s ease, color 0.15s ease;
      }
      .dt-tool:hover {
        background: rgba(24, 24, 28, 0.04);
        color: var(--ink);
      }
      .dt-tool svg {
        width: 13px;
        height: 13px;
      }
      .dt-tool-caret {
        opacity: 0.55;
      }
      /* send button — peach until input, then ink */
      .dt-send {
        width: 28px;
        height: 28px;
        border-radius: 50%;
        border: 0;
        background: rgba(216, 169, 116, 0.35);
        color: var(--paper);
        display: inline-flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: background 0.18s ease, transform 0.15s ease, box-shadow 0.18s ease;
        box-shadow: 0 4px 12px rgba(216, 169, 116, 0.18);
      }
      .dt-send svg {
        width: 14px;
        height: 14px;
      }
      .dt-send:hover {
        background: rgba(216, 169, 116, 0.55);
      }
      .dt-send.is-active {
        background: var(--ink);
        box-shadow: 0 4px 14px rgba(24, 24, 28, 0.25);
      }
      .dt-send.is-active:hover {
        background: #000;
        transform: translateY(-1px);
      }

      @media (max-width: 880px) {
        .dt-app {
          grid-template-columns: 1fr;
          height: auto;
          max-height: none;
        }
        .dt-side {
          display: none;
        }
      }
      @media (max-width: 640px) {
        .dt-topbar-actions .dt-act:nth-child(-n + 2) {
          display: none;
        }
        .dt-act span:not(.dt-act-glyph) {
          display: none;
        }
        .dt-tool span {
          display: none;
        }
        .dt-pill-model {
          max-width: 120px;
        }
        .dt-pill-model .dt-pill-label {
          max-width: 70px;
        }
      }

      /* ───────── Dark theme polish ───────── */
      [data-theme="dark"] .nav {
        background: rgba(13, 13, 16, 0.78);
      }
      [data-theme="dark"] .showcase-chrome {
        background: rgba(22, 22, 26, 0.7);
      }
      [data-theme="dark"] .hero-wordmark {
        filter: invert(1) hue-rotate(180deg) brightness(1.12);
      }
      [data-theme="dark"] .showcase-frame {
        box-shadow:
          0 1px 0 rgba(255, 255, 255, 0.04) inset,
          0 40px 80px -44px rgba(0, 0, 0, 0.7);
      }
      [data-theme="dark"] .btn-primary:hover,
      [data-theme="dark"] .dt-send.is-active:hover {
        background: #f1ede4;
        color: #0d0d10;
      }
      [data-theme="dark"] .feature:hover,
      [data-theme="dark"] .eco-row:hover,
      [data-theme="dark"] .dt-tool:hover {
        background: rgba(123, 190, 234, 0.08);
      }
      [data-theme="dark"] .install-code {
        background: rgba(22, 22, 26, 0.74);
      }
      [data-theme="dark"] .install-tabs {
        background: rgba(241, 237, 228, 0.045);
        border-color: rgba(241, 237, 228, 0.06);
      }
      [data-theme="dark"] .install-tab.is-active {
        background: rgba(241, 237, 228, 0.1);
        box-shadow:
          0 1px 1px rgba(0, 0, 0, 0.2),
          0 2px 8px -2px rgba(0, 0, 0, 0.3);
      }
      [data-theme="dark"] .dt-side {
        background: #141418;
      }
      [data-theme="dark"] .dt-side-item:hover,
      [data-theme="dark"] .dt-side-item.is-active,
      [data-theme="dark"] .dt-side-sub-item:hover,
      [data-theme="dark"] .dt-side-gh:hover {
        background: rgba(241, 237, 228, 0.06);
      }
      [data-theme="dark"] .dt-act,
      [data-theme="dark"] .dt-composer,
      [data-theme="dark"] .dt-pill {
        border-color: var(--rule);
      }

      /* ───────── Footer ───────── */
      .footer {
        max-width: 1280px;
        margin: 96px auto 0;
        padding: 64px 32px 48px;
      }
      .footer-inner {
        display: grid;
        grid-template-columns: 260px 1fr;
        gap: 48px;
        margin-bottom: 64px;
      }
      .footer-brand {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        text-decoration: none;
        color: var(--ink);
        align-self: start;
      }
      .footer-brand img {
        width: 32px;
        height: 32px;
      }
      .footer-brand span {
        font-family: var(--font-display);
        font-style: italic;
        font-weight: 500;
        font-size: 22px;
      }
      .footer-cols {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 40px;
      }
      .footer-col h4 {
        margin: 0 0 16px;
        font-family: var(--font-mono);
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: var(--ink-faint);
      }
      .footer-col a {
        display: block;
        margin-bottom: 10px;
        text-decoration: none;
        color: var(--ink-soft);
        font-size: 14px;
        transition: color 0.15s ease;
      }
      .footer-col a:hover {
        color: var(--ink);
      }
      .footer-rule {
        height: 1px;
        background: var(--rule);
        margin-bottom: 28px;
      }

      /* ───────── Heartbeat (single editorial line, mono) ───────── */
      .footer-pulse {
        margin: 0 0 24px;
        display: inline-flex;
        align-items: center;
        flex-wrap: wrap;
        gap: 7px;
        font-family: var(--font-mono);
        font-size: 11.5px;
        letter-spacing: 0.02em;
        color: var(--ink-faint);
        opacity: 1;
        transition: opacity 0.35s ease;
      }
      .footer-pulse[data-hidden="true"] {
        opacity: 0;
        pointer-events: none;
        position: absolute;
      }
      .footer-pulse-dot {
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: var(--accent);
        box-shadow: 0 0 0 3px var(--accent-soft);
        animation: dtPulse 2.4s ease-in-out infinite;
      }
      @keyframes dtPulse {
        0%, 100% { opacity: 1; transform: scale(1); }
        50%      { opacity: 0.45; transform: scale(0.85); }
      }
      .footer-pulse .sep {
        color: var(--ink-faint);
        opacity: 0.5;
      }
      .footer-pulse .num {
        color: var(--ink-soft);
        font-weight: 600;
        font-variant-numeric: tabular-nums;
      }
      .footer-pulse .num > [data-pulse] {
        color: var(--ink);
      }
      .footer-pulse-tag {
        color: var(--ink) !important;
        font-weight: 600;
        text-decoration: none !important;
        border-bottom: 1px solid var(--rule);
        padding-bottom: 1px;
        transition: border-color 0.15s ease;
      }
      .footer-pulse-tag:hover {
        border-color: var(--ink);
      }

      .footer-fine {
        display: flex;
        justify-content: space-between;
        font-family: var(--font-mono);
        font-size: 11.5px;
        color: var(--ink-faint);
        letter-spacing: 0.02em;
      }
      .footer-fine a {
        color: var(--ink-soft);
        text-decoration: none;
      }
      .footer-fine a:hover {
        color: var(--ink);
      }
      @media (max-width: 720px) {
        .footer-inner {
          grid-template-columns: 1fr;
          gap: 32px;
        }
        .footer-cols {
          grid-template-columns: repeat(2, 1fr);
          gap: 24px;
        }
        .footer-fine {
          flex-direction: column;
          gap: 8px;
        }
      }
    </style>

    <script>
      // ───────── GitHub star count ─────────
      (() => {
        const targets = document.querySelectorAll<HTMLElement>("[data-gh-stars]");
        if (!targets.length) return;
        const cacheKey = "deeptutor-gh-stars";
        const ttl = 60 * 60 * 1000; // 1h
        const format = (n: number) =>
          n >= 1000 ? (n / 1000).toFixed(n >= 10000 ? 1 : 2).replace(/\.?0+$/, "") + "k" : String(n);
        const paint = (n: number) => {
          const text = format(n);
          targets.forEach((el) => (el.textContent = text));
        };
        try {
          const raw = localStorage.getItem(cacheKey);
          if (raw) {
            const cached = JSON.parse(raw);
            if (cached && typeof cached.n === "number" && Date.now() - cached.t < ttl) {
              paint(cached.n);
            }
          }
        } catch {}
        fetch("https://api.github.com/repos/HKUDS/DeepTutor")
          .then((r) => (r.ok ? r.json() : null))
          .then((data) => {
            if (!data || typeof data.stargazers_count !== "number") return;
            paint(data.stargazers_count);
            try {
              localStorage.setItem(
                cacheKey,
                JSON.stringify({ n: data.stargazers_count, t: Date.now() }),
              );
            } catch {}
          })
          .catch(() => {});
      })();

      // ───────── Install tabs ─────────
      const installRoot = document.getElementById("install");
      if (installRoot) {
        const tabs = installRoot.querySelectorAll<HTMLButtonElement>(".install-tab");
        const panels = installRoot.querySelectorAll<HTMLElement>(".install-panel");
        tabs.forEach((tab) => {
          tab.addEventListener("click", () => {
            const target = tab.dataset.target;
            tabs.forEach((t) => {
              const on = t === tab;
              t.classList.toggle("is-active", on);
              t.setAttribute("aria-selected", on ? "true" : "false");
            });
            panels.forEach((p) => {
              p.classList.toggle("is-active", p.dataset.panel === target);
            });
          });
        });
      }

      // ───────── Landing preferences ─────────
      const themeKey = "starlight-theme";
      const themeQuery = "(prefers-color-scheme: light)";
      const readThemePreference = () => {
        try {
          const stored = localStorage.getItem(themeKey);
          return stored === "light" || stored === "dark" ? stored : "auto";
        } catch {
          return "auto";
        }
      };
      const resolvedTheme = (preference: string) => {
        if (preference === "light" || preference === "dark") return preference;
        return window.matchMedia(themeQuery).matches ? "light" : "dark";
      };
      const applyThemePreference = (preference: string) => {
        document.documentElement.dataset.theme = resolvedTheme(preference);
        document.querySelectorAll<HTMLSelectElement>("[data-theme-select]").forEach((select) => {
          select.value = preference;
        });
      };
      const persistThemePreference = (preference: string) => {
        try {
          if (preference === "light" || preference === "dark") {
            localStorage.setItem(themeKey, preference);
          } else {
            localStorage.setItem(themeKey, "");
          }
        } catch {}
      };

      applyThemePreference(readThemePreference());

      document.querySelectorAll<HTMLSelectElement>("[data-theme-select]").forEach((select) => {
        select.addEventListener("change", () => {
          persistThemePreference(select.value);
          applyThemePreference(select.value);
        });
      });

      window.matchMedia(themeQuery).addEventListener("change", () => {
        if (readThemePreference() === "auto") applyThemePreference("auto");
      });

      document.querySelectorAll<HTMLSelectElement>("[data-locale-select]").forEach((select) => {
        select.addEventListener("change", () => {
          const nextPath = select.value;
          if (nextPath && nextPath !== window.location.pathname) {
            window.location.assign(`${nextPath}${window.location.search}${window.location.hash}`);
          }
        });
      });
    </script>
  </body>
</html>
